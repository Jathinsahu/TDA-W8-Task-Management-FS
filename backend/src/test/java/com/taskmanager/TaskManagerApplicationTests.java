package com.taskmanager;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Basic integration test that verifies the Spring Boot application
 * context loads successfully. This test checks that:
 * - All beans are properly configured
 * - Dependencies are correctly wired
 * - The application can start without errors
 */
@SpringBootTest
class TaskManagerApplicationTests {

    @Test
    void contextLoads() {
        // This test passes if the application context loads without errors
        // It validates that all configurations, beans, and dependencies are correct
    }
}
